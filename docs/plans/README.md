# Plans

One document per substantial change, written **before** the code and committed
**with** it.

## Why these are in the repository

Most of the reasoning behind a change never reaches the diff. The diff shows a
grace period being deleted; it does not show that two players' save files were
read tick by tick to work out that `11118 = 1853 × 6` meant one client had
stalled on a turn boundary while `14084 = 2347 × 6 + 2` meant the other was
free-running. That reasoning is the expensive part, it is what makes the next
bug in the same area cheap to find, and it evaporates the moment the session
that produced it ends.

Commit messages carry the *what*. `bug-fixed.md` carries the confirmed
*outcome*. These carry the *investigation* — the evidence, the reconstruction,
the alternatives weighed and rejected, and the things found along the way that
were deliberately not fixed.

## Conventions

- **One file per change**, named for the problem rather than the fix:
  `online-multiplayer-desync.md`, not `add-start-barrier.md`. The problem is
  what somebody will search for later.
- **Kebab-case**, matching `docs/performance-optimization-plan.md`.
- **Committed on the same branch as the code it describes**, so the plan and the
  diff arrive in one reviewable unit and stay together in the history.
- **Left alone once merged.** A plan is a record of what was believed and
  decided at a point in time. If a later change proves part of it wrong, that
  belongs in the *new* plan, which can link back — rewriting history to look
  correct in hindsight destroys the only thing these are for.

## What belongs in one

The section that matters most is the evidence. A plan that says "fix the
multiplayer desync" is worth nothing; one that says "here are the two save
files, here is the arithmetic that identifies which failure mode each client
hit, and here is why that rules out the other three candidate causes" is worth
keeping.

Findings that were investigated and *not* fixed matter just as much — they are
the difference between "nobody noticed" and "noticed, judged lower priority,
here is the reasoning". Record them with enough detail to act on later.

## Index

| Plan | Change |
|---|---|
| [online-multiplayer-desync.md](online-multiplayer-desync.md) | Two clients ran independent simulations of the same map; three fail-open paths in the lockstep start barrier, late-join and disconnect handling. |
| [online-multiplayer-quorum-and-rejoin.md](online-multiplayer-quorum-and-rejoin.md) | Follow-up: the *running* match had the identical fail-open the previous fix closed only at *start* — a dropped peer let the survivor free-run, rejoining never actually worked, and a stall got the player ejected and re-doubled the world on return. |
| [online-multiplayer-protocol-handshake.md](online-multiplayer-protocol-handshake.md) | Follow-up: neither of the above wire-format changes was guarded by any check that two peers agree on the protocol at all — added a hardcoded, hand-bumped `PROTOCOL_VERSION` checked in both directions at connect time. |
| [ai-base-spawn-degenerate-fallback.md](ai-base-spawn-degenerate-fallback.md) | A team's degenerate "no dry land on this exact bearing" fallback returned the unconditional map centre, which the nudge-selection loop one level up could not distinguish from a genuine coastal point — scoped the fallback to sweep within the team's own slice for dry ground before giving up. |
| [vehicle-builder.md](vehicle-builder.md) | A parametric vehicle editor behind God Mode, reusing the game's own mesh builder. The design problem was not the UI but keeping author-built vehicles out of lockstep matches, where only `defId` strings cross the wire — solved with a fail-closed allowlist of offline modes. |
| [minimap.md](minimap.md) | A bottom-right minimap over the fog's own CPU grid. Two bugs that drew wrong rather than erroring: team colours are numbers and a numeric canvas `fillStyle` is silently ignored, and there is no honest camera footprint to draw when the view sees the horizon. Also carries the base-defense roadmap. |
| [mobile-ui-scaling.md](mobile-ui-scaling.md) | The vanishing hamburger menu was a real bug: nothing restricted native pinch-zoom, and touch-action:none only covered the canvas, so a pinch near a toggle button reached the browser's page zoom and shifted every fixed-position element. Fixed both drawers being open simultaneously and unreadable on a narrow phone along the way, found only by screenshotting the sizing fix. |
| [base-defenses.md](base-defenses.md) | A gun turret and a sensor tower, deployed from a vehicle because `canPlaceAt` confines structures to a radius-40 base pad holding two or three buildings — so nothing placed the normal way could ever defend a perimeter. Extracted the turret rig so a vehicle and an emplacement cannot drift apart. |
| [custom-vehicle-production.md](custom-vehicle-production.md) | Making an author-built vehicle buildable from a factory. `producedBy` existed on every def and was read nowhere — the live link was the structure's `produces` array, generated into commands at module import, which a player can never reach. Made the field load-bearing. |
| [tracked-vehicles.md](tracked-vehicles.md) | Tank tracks with suspension. The belt geometry was the easy half — a track has no steered axle, so `steeringWheelbase` returns Infinity and every consumer concluded a tank could only drive straight; needed a pivot-steer model alongside the bicycle one. |
| [online-multiplayer-lighting-desync.md](online-multiplayer-lighting-desync.md) | Vehicle/building lighting looked different between online players. Traced every "normal play" lighting path and found it already tick-deterministic; the real gap was two debug-only headlight toggles writing to local-only state, unlike their day/night siblings which were already locked during online play — locked them the same way. |
| [online-multiplayer-mutual-stall.md](online-multiplayer-mutual-stall.md) | In progress: both clients STALLED at once on a two-tab-one-machine test, after a genuine earlier `AGREED` — rules out the three fixes above and a per-process room split, leading hypothesis is `requestAnimationFrame` throttling on a backgrounded tab starving `LockstepSession`. Added diagnostic logging (previously none existed); root cause not yet confirmed from a live repro. |
| [portal-landing-button-row.md](portal-landing-button-row.md) | The landing screen's unused background photo and its five-button row (sign-in plus the three modes plus a God-Mode button gated on one account's email). |
| [ai-defense-and-sell.md](ai-defense-and-sell.md) | Follow-up to `base-defenses.md`: the AI now builds a field engineer and drives it out to deploy a defense (with a distance floor a human player doesn't need but a freshly-spawned AI engineer does), and both defenses can be sold back for a health-scaled refund. |
| [custom-vehicles-online.md](custom-vehicles-online.md) | Author-built vehicles in online matches and in AI hands. Ids were name slugs, so two accounts' different vehicles could share one — content-addressed them instead, which also makes the name free text. The match now owns a vehicle set pinned from the host's loadout and relayed in `welcome` (protocol v2), and the editor's slider ranges became binding on both sides rather than being HTML attributes nothing enforced. |
| [harvester-collision-avoidance-study.md](harvester-collision-avoidance-study.md) | An investigation, not a fix: four harvesters and one depot, run for ten simulated minutes. Two ordinary collisions in the approach corridor left **all four** permanently frozen for 83% of the run — traced to `_onAbandoned`'s `TO_BASE` branch, which never bans its destination and so re-ran an identical five-angle detour sweep 256 times without moving. |
| [facility-clearance-control.md](facility-clearance-control.md) | Follow-up to the study: harvesters and repair-bay traffic now request clearance before entering a facility's approach corridor. The game already had this built twice — `harvesterAI` and `repairController` each carried their own claim + allocator + sweep, fixing the same bugs out of step — so this unifies them behind one controller whose ledger is *derived* from the vehicles each tick rather than stored on structures. The first version deadlocked in a browser run the unit tests could not see. |
| [vehicle-collision-avoidance.md](vehicle-collision-avoidance.md) | Vehicles clumped instead of flowing around each other — the existing avoidance check only ever meant "stop." Kept the cone-detection trigger, replaced the stop with a lateral steering-offset swerve; found and fixed a case where the swerve itself could abandon a perfectly good order over a transient rock outcrop only the nudge, not the real target direction, ever faced. |
| [ai-commander-capabilities.md](ai-commander-capabilities.md) | An investigation, not a fix: what the AI opponent can actually do. It harvests, builds and attacks on a flat headcount with **no comparison to the enemy's strength anywhere**, and has **no retreat** — `combatController` stamps `threatUntil` on every damaged unit and only `harvesterAI` reads it, so a gun platform fights to zero. Difficulty changes cadence and caps, never resources or stats. |
| [ai-commander-overhaul.md](ai-commander-overhaul.md) | Follow-up to the above: a posture layer that measures both armies with a generic `damage/fireInterval × maxHealth` power metric (reading the same `turret` block a vehicle and a gun turret both carry, so nothing is keyed by unit id), commits only above a strength ratio with hysteresis against flapping, strikes once at a newly-scouted weakly-defended base, and pulls damaged units back to heal — the last of which needed no new state machine, because `repairController`'s generic auto-queue already did it at a lower threshold. |
| [harvester-field-selection.md](harvester-field-selection.md) | Follow-up to the AI overhaul: on `hard` the AI built zero combat units in 45 minutes because a captured credit trace went flat at 1000cr for 90+ seconds. Traced to two harvesters converging on one crystal field — its regen ceiling (6/s) can't keep up with two harvesters filling at once (96/s) — and fixed with an additive field-selection tier, shared code so it helps players too. Verified the fix works, and found a second, unrelated freeze downstream of it: a harvester's live order silently stopping with zero displacement for 400+ seconds, confirmed present on the unmodified baseline too and not investigated further here. |
| [statistics-screen.md](statistics-screen.md) | The left drawer became a two-item chooser (Statistics / World Settings), plus a live per-team stats page. Three findings shaped it: `creditsEarned` is not harvest income (`earn()` is also called for sell refunds, so Score needed its own field); a unit's kills and earnings die with the unit, so the tallies are written at the existing increment sites rather than rescued at destroy time; and `team.stats` gained its first nested values, making snapshot's shallow spread a latent aliasing hazard. Laid out with stats down the side and teams across the top — a column per stat overflowed a 320px drawer, measured in a browser. |
| [ai-driving-fixes.md](ai-driving-fixes.md) | Ten defects a player's 44-minute expert diagnostic exposed, each of which stopped an AI unit permanently. Scoped as four — a one-unit-wide dead band where arrival was measured to the dock and release to the building centre (two teams finished on 320cr and 0cr because of it); `_driveOneScout` having no `repair` guard, so aiCommander and repairController steered the same scout and neither's give-up was ever reachable (detour counts of 74 against a six-angle ladder); `markDocked` stopping the clearance lease one leg before service actually starts (a scout held a bay for 372 seconds from 228 units away); and `combatCap` being a per-unit-id allowance, so every AI bought 7 scouts it then refused to field. Fixing those four unmasked five more, each root-caused against a fresh re-run: an escape cooldown shorter than the reverse it gates; a mechanical hold with no bound; a waypoint leg that returned before anything could re-issue its lost order; a hold that wiped the no-progress evidence every time it re-armed; a detour ladder that reset itself on success so its give-up was dead code; and a loaded harvester joining a repair queue instead of finishing the delivery. Final re-run is clean on all six of the save's failure signatures, with the worst team's income up from 0cr to 13,440cr. |
| [tracked-vehicles-forward-driving-and-sell.md](tracked-vehicles-forward-driving-and-sell.md) | Follow-up: vehicles were still spending real time driving backwards after the ten fixes above, traced to the detour ladder's own last angles pointing behind the vehicle — and to `tracked-vehicles.md`'s pivot-steering engine having shipped with no catalog vehicle ever opting into it. Added three tracked vehicles (steeper climb, 2–3× cost) and gated both the sharp-turn reverse and the detour ladder on `inst.tracked`, so a tracked vehicle pivots in place instead of ever needing a backward-facing angle. Also added a universal, age/condition/kill-scored vehicle sell command and a dusk/night/dawn flare on the new heavy tank, which reveals its locked target's surroundings permanently — the engine's only reveal primitive, since no temporary/decaying fog state exists to reuse. |
| [itch-io-cross-site-session.md](itch-io-cross-site-session.md) | Follow-up: with CORS fixed, sign-in on itch.io appeared to work and online play still refused it. Two consecutive `/auth/me` calls returning *different* CSRF tokens proved the browser was discarding the session cookie — itch.io serves the game third-party, and Safari's ITP blocks such cookies unconditionally, `SameSite=None` notwithstanding. Added an opt-in bearer-token path (cookie untouched for the main site, which keeps its httpOnly session), carried on the WebSocket as a subprotocol rather than a query param so a live credential never lands in an access log. The negative control earned its keep: it passed against a broken `subprotocolToken`, exposing a real gap in the test. |
| [itch-io-multiplayer-cors.md](itch-io-multiplayer-cors.md) | Online multiplayer didn't connect from the itch.io build. Reproduced live by reading the console inside itch.io's actual game iframe: the API's CORS allow-list was hard-locked to the main site's single origin, rejecting every request from `html-classic.itch.zone`. A second, latent bug sat underneath — the session cookie is genuinely cross-*site* there (different registrable domain), so `COOKIE_SAMESITE=lax` would have silently dropped it even after CORS was fixed. Made `CORS_ORIGIN` a comma-separated list and turned on the already-built `COOKIE_SAMESITE=none` support; both are config-only, no protocol changes. |
| [fps-regression-second-pass.md](fps-regression-second-pass.md) | 60fps to 22fps across 74 commits, from three stacked causes — and two of them were named in advance, in writing, by `fps-regression.md` and shipped anyway. The headlight pool went 4 to 32 real SpotLights on reasoning that compared a *rig* count against a cost curve measured in *lights* (32 is double the documented knee, not inside it); the sound cache key was fixed without the pre-warm the first document called its precondition, putting synchronous `OfflineAudioContext` bakes inside gameplay frames; and an O(n²) flow-field solve landed on a per-tick path because every shell impact invalidated its cache, the AI army it feeds had been dead code at the baseline, and its 24-entry cache was sized for 4 teams rather than 20. The heap and broad-phase replacements are proved *byte-identical* to what they replaced, because pair and settle order are part of a lockstep simulation's result — the negative control for the heap fails the parity test while all 11 existing NavGrid tests still pass. |
| [split-brain-invisible-to-the-hash.md](split-brain-invisible-to-the-hash.md) | Two players, one match, two different worlds — each finding "an enemy base" that was not the other's while every number on screen agreed. Vehicles were hashed with their coordinates; **structures were not**, and a base station is a structure, so the only cross-client check the game has was blind to where every building on the map stood. Nothing compared the islands either: only the seed crosses the wire while every other terrain parameter comes from each client's own bundle. Fixed both (protocol 4 → 5), plus a separate real bug found on the way — teams sized from the lobby's *capacity* rather than its roster, which spawns bases for seats nobody sits in. The trigger for this particular match is **still unidentified**, and the doc says so rather than claiming a fix it cannot demonstrate. |
| [portal-picker-back-buttons.md](portal-picker-back-buttons.md) | Sign In, Sandbox and Multiplayer AI were one-way doors — a grep confirmed `difficultyScreen.js`/`aiDifficultyScreen.js` had no back mechanism at all, unlike `LobbyScreen`'s existing `onBack`. Added a `← Back` button to all three, following `LobbyScreen`'s own pattern, and extracted the duplicated "rebuild and reveal the portal" sequence into one `returnToPortal()` helper in `main.js`. |
| [itch-io-portal-asset-paths.md](itch-io-portal-asset-paths.md) | The itch.io build showed a broken portal logo. Root-absolute asset paths (`/foo.png`) aren't rewritten by Vite's `base` config — fine on the root nginx deploy, a 404 on itch.io's non-root hosting. Fixed the `<img>` src with `import.meta.env.BASE_URL`; the CSS background needed a second fix on top, since a relative `url()` inside a custom property resolves against the *stylesheet's* location, not the document's — caught only by simulating itch.io's real non-root URL shape locally. |
| [skirmish-settings-restriction.md](skirmish-settings-restriction.md) | The hamburger menu still drew all nine World Settings groups mid-skirmish — `simState()` only *disabled* the subset of controls that write simulation state, and only during an online match, leaving Ground/Water/Game-debug (which never touch sim state) fully live and every other group visible-but-greyed. Filtered `buildSchema()`'s own output down to Performance/Camera/Sound for both online and vs-AI, rather than widening the per-control lock — hides the wall instead of merely greying it out. |
| [readd-save-load-vs-ai.md](readd-save-load-vs-ai.md) | Follow-up: Save/Load was hidden in vs-AI along with online, but the actual risk (`simState()`'s own reasoning) is a *peer* silently diverging — vs-AI has none, and `core/snapshot.js` fully round-trips `game.aiCommanders`, so the restriction there was never protecting against anything. Split the one shared group list into two mode-specific ones, and centralized the chooser's hint text in the same file as the list it describes, closing a drift risk the original PR's hand-duplicated hint already had. |
| [new-players-are-taught-nothing.md](new-players-are-taught-nothing.md) | Nothing in the game taught the command-ring gesture — a long-press/double-click with no button, no menu entry and no affordance — without which a player cannot deploy their base and so cannot play. Added one-at-a-time dismissible hints, paced by an observe-and-diff engine copied from `chatter.js` (reads sim state, writes nothing, driven from the existing half-second poll), gated on a local match counter rather than the account, because a first-time player is the person least likely to have registered. The design rule that matters is `retiredWhen`: a hint the player outgrows retires unfired, because competence is the real dismissal. Two bugs were invisible to the unit tests and found only by driving a real browser — sandbox auto-opens the vehicle picker, which both suppressed every hint forever and credited the player with opening a drawer the game opened. |
| [mobile-drawers-bury-the-hud.md](mobile-drawers-bury-the-hud.md) | On a phone both hamburger drawers are `min(88vw, 340px)`, and every world overlay had been taught to dodge only the drawer sharing *its own* screen edge — correct for a 320px desktop panel, wrong the moment one spans both. Measured at 320/360/390px: the radio feed 86-100% covered, the HUD 90%, the minimap 67-78%, and the version badge (z-index 45, so *above* the drawers) landing on the panel's own "Menu" heading. Replaced the per-edge rules with one fade, moved the build stamp into the panel's diagnostics line, and fixed two touch-only text losses — vehicle stats revealed by `:hover` alone, and the unlock condition ellipsised to "Locked — char…". The lock-line negative control passed at first, which exposed an assertion comparing `scrollHeight` on a line that can only clip horizontally; sharpening it caught a real bug in the fix itself. |
