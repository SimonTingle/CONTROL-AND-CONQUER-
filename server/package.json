{
  "name": "procedural-terrain-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Accounts, cloud saves, and match relay for the procedural terrain RTS. Deployed as its own CapRover app alongside the static frontend.",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "migrate": "node src/db/migrate.js"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/cors": "^10.0.1",
    "@fastify/rate-limit": "^10.2.1",
    "@fastify/websocket": "^11.0.1",
    "argon2": "^0.41.1",
    "fastify": "^5.2.0",
    "pg": "^8.12.0",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": ">=20"
  }
}
